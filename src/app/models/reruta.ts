export class Ruta {
    id: number;
    ruta: string;
    estado: string;

    constructor(id: number, ruta: string, estado: string) {
    this.id = id;
    this.ruta = ruta;
    this.estado = estado;
    }
}
